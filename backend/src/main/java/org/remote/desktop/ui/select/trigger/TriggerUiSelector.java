package org.remote.desktop.ui.select.trigger;

import org.remote.desktop.model.EAxisEaser;
import org.remote.desktop.model.ETriggerEvent;
import org.remote.desktop.ui.select.DuoSelectApplication;

public class TriggerUiSelector extends DuoSelectApplication<ETriggerEvent, EAxisEaser> {
}
